<UserControl
    x:Class="SellIt_UWP.Views.FollowingCommandsUC"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SellIt_UWP.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Views/Styles/LoginUCStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="8*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Sélectionner un client" Grid.Row="0"/>
        <ComboBox x:Name="comboBoxClient"  ItemsSource="{Binding ClientList.Firstname,Mode=OneTime}" Grid.Row="1">
            <ComboBoxItem />
        </ComboBox>
        <TextBlock Text="Sélectionner une commande" Grid.Row="3"/>
        <ComboBox x:Name="comboBoxOrder" Grid.Row="4">
            <ComboBoxItem />
        </ComboBox>
    </Grid>
</UserControl>
